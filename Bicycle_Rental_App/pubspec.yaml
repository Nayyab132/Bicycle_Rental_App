name: bicycle_rental_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.6.0           # Updated Firebase Core
  firebase_auth: ^5.3.1            # Updated Firebase Authentication
  firebase_storage: ^12.3.3         # Added Firebase Storage
  cloud_firestore: ^5.4.4           # Updated Cloud Firestore
  google_sign_in: ^6.2.1
  logger: ^2.4.0
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/google_logo.png  # Ensure this line is properly indented
